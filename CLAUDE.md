# Claude Code project entry point

Read `.agents/project.md` before changing this repo. That file is the shared project memory for coding assistants.

Claude Code-specific tooling lives in `.claude/`; shared agent prompts and reusable instructions live in `.agents/`.
