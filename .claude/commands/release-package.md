---
description: Add a changeset for pending mp-mcp changes and verify the package is shippable. Does not publish.
argument-hint: [bump-type: patch|minor|major] [summary]
---

Read `.agents/skills/release-package/SKILL.md` and follow it exactly, using `$ARGUMENTS` as the optional bump type and summary.
