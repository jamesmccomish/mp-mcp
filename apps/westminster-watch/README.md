# westminster-watch

Planned consumer app for the `mp-mcp` server. Stub only in this build —
nothing implemented yet.

The intended demo is a "postcode → MP report card" flow that exercises
`parliament_find_member`, `parliament_member_overview`, and
`parliament_topic_tracker` end-to-end. Design will live in a separate plan
once the MCP server stabilises.
