# Codex project entry point

Read `.agents/project.md` before changing this repo. That file is the shared project memory for coding assistants.

Codex-specific tooling lives in `.codex/`; shared agent prompts and reusable instructions live in `.agents/`.
